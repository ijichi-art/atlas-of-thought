# Place icons here:
# - icon.png  (1024x1024 PNG, used for Linux + as source for the others)
# - icon.icns (macOS, generate via:  iconutil -c icns icon.iconset)
# - icon.ico  (Windows, generate via:  imagemagick convert icon.png ... icon.ico)
#
# electron-builder will fall back to a generic icon if these are missing,
# so the launch isn't blocked on artwork.

